import { Listener } from "./base-listner-class";
import { Message } from "node-nats-streaming";
import { TicketCreatedEvent } from "../event-types";
import { Subjects } from "../subjects";
export declare class TicketCreatedListner extends Listener<TicketCreatedEvent> {
    subject: Subjects.TicketCreated;
    queueGroupName: string;
    onMessage(msg: Message, data: TicketCreatedEvent['data']): void;
}
