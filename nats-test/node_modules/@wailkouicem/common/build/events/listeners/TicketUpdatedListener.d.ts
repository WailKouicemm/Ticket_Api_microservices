import { Message } from "node-nats-streaming";
import { TicketUpdatedEvent } from "../event-types";
import { Subjects } from "../subjects";
import { Listener } from "./base-listner-class";
export declare class TicketUpdatedListener extends Listener<TicketUpdatedEvent> {
    subject: Subjects.TicketUpdated;
    queueGroupName: string;
    onMessage(msg: Message, data: TicketUpdatedEvent["data"]): void;
}
