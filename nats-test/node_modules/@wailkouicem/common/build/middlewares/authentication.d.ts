import { NextFunction, Request, Response } from 'express';
interface user {
    id: string;
    email: string;
}
declare global {
    namespace Express {
        interface Request {
            user?: user;
        }
    }
}
export declare const authMiddlware: (req: Request, res: Response, next: NextFunction) => Promise<void>;
export {};
